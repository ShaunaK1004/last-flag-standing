# V35.2 Priority Spawn Fix

- Priority member/Super Chat/Super Sticker events now ALWAYS spawn a NEW custom flag for the triggering @user.
- Existing normal country flags are never upgraded into Alpha for priority events.
- Existing custom flags of the same country are never reused for a new priority event.
- This preserves the user ownership association and ensures the reward appears as a separate viewer-triggered flag.
